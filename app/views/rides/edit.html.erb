<h1>Edit a Ride</h1>
<%= form_for(@ride, html: {class: "form-horizontal"}) do |f| %>
	<fieldset>
		<div class="form-group">
			<%= f.label :name, "Ride Name", class: "col-lg-2 control-label" %>
			<div class="col-lg-10">
				<%= f.text_field :name, class: "form-control" %>
			</div>
		</div>

		<%# <div class="form-group"> %>
		<%# 	<%= f.label :RideSponsorId, "Ride Sponsor ID", class: "col-lg-2 control-label" %1> %>
		<%# 	<div class="col-lg-10"> %>
		<%# 		<%= f.number_field :RideSponsorId, class: "form-control" %1> %>
		<%# 	</div> %>
		<%# </div> %>

		<div class="form-group">
			<%= f.label :date, "Ride Date", class: "col-lg-2 control-label" %>
			<div class="col-lg-10">
				<%= f.date_field :date, class: "form-control" %>
			</div>
		</div>

		<div class="form-group">
		<%= f.label :launch_time, "Ride Launch Time", class: "col-lg-2 control-label" %>
			<div class="col-lg-10">
				<%= f.time_field :launch_time, class: "form-control" %>
			</div>
		</div>


		<div class="form-group">
			<%= f.label :terrain, "Ride Terrain", class: "col-lg-2 control-label" %>
			<div class="col-lg-10">
				<%= f.text_field :terrain, class: "form-control" %>
			</div>
		</div>

		<%# <div class="form-group"> %>
		<%# 	<%= f.label :RideLeader, "Rode Leader", class: "col-lg-2 control-label" %1> %>
		<%# 	<div class="col-lg-10"> %>
		<%# 		<%= f.number_field :RideLeader, class: "form-control" %1> %>
		<%# 	</div> %>
		<%# </div> %>

		<div class="form-group">
			<%= f.label :description, "Ride Description", class: "col-lg-2 control-label" %>
			<div class="col-lg-10">
				<%= f.text_field :description, class: "form-control" %>
			</div>
		</div>

		<%# We need to verify what is going on with the RideStartLocation %>
		<%# Is it a separate model that is related to the Ride model? %>
		<%# <%= f.label :RideStartLocation, "Ride Start Location" %1> %>
		<%# <%= f.text_field :RideStartLocation %1> %>

		<div class="form-group">
			<div class="col-lg-10 col-lg-offset-2">
				<%= f.submit "Create Ride", class: "btn btn-primary" %>
			</div>
		</div>
	</fieldset>
<% end %>	
